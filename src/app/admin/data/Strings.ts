
export class Strings {
  static authenticatedUsername = "authenticatedUsername"
  static authenticatedPassword = "authenticatedPassword"
  static isAuthenticated = "isAuthenticated"
  static authenticatedUserJwtToken = "authenticatedUserJwtToken"
  static authenticatedUserFirstname = "authenticatedUserFirstname"
  static authenticatedUserLastnames = "authenticatedUserLastnames"
}
